url: ${APP_URL || "https://localhost:3000"}/testing/sign-in?redirect=/testing/seed-credentials?redirect=/profile&worker=${WORKER_INDEX || "0"}
name: Test SportLots Credentials
tags:
  - credentials
  - profile
  - serial-marketplace
---
- launchApp
- extendedWaitUntil:
    visible: "Neon Binder"
    timeout: 45000

# The sign-in URL routes through /testing/seed-credentials, which calls the
# auth-scoped seedMyTestCredentials action server-side. The action reads the
# dev BSC/SportLots credentials from Convex server env vars and stores them
# under the signed-in user. No secret passes through Maestro -e env.
# After seeding, /testing/seed-credentials forwards to /profile.
- extendedWaitUntil:
    visible: "Profile Settings"
    timeout: 30000

# --- Test the credentials ---
# Switch to SportLots tab and tap "Test Credentials".
# The seeded creds are real, so the test should succeed.
# Center the "Sportlots" tab rather than stopping at "BuySportsCards Credentials".
# The tab row sits just below the h2 heading; scrolling only to the heading leaves
# the Sportlots tab at the 629px viewport bottom edge, causing tapOn to trigger
# Maestro's scrollToPoint → CDP empty-response flake. Centering the tab puts it
# near y≈315 so the subsequent tapOn needs no implicit scroll.
- scrollUntilVisible:
    element:
      text: "Sportlots"
    centerElement: true
    timeout: 30000
- tapOn: "Sportlots"
- scrollUntilVisible:
    element:
      text: ".*Sportlots Credentials.*"
    timeout: 10000
- scrollUntilVisible:
    element:
      text: "Test Credentials"
    timeout: 10000
- tapOn: "Test Credentials"
# 30s exception: a COLD marketplace login is a real ~22s Puppeteer/SSO round-trip (cached is <1s).
# This is the ONE sanctioned >10s wait (the standing rule is 10s) — do NOT copy this ceiling elsewhere.
# scrollUntilVisible both waits for the element AND scrolls it into the viewport (the success
# banner renders below the button, just off the visible fold), so the assertVisible that follows
# is guaranteed to see it without a separate extendedWaitUntil.
- scrollUntilVisible:
    element:
      text: ".*Sportlots account authenticated successfully.*|.*Credentials saved successfully.*"
    timeout: 30000
- assertVisible:
    text: ".*Sportlots account authenticated successfully.*|.*Credentials saved successfully.*"

# --- Verify the Clear Credentials button works ---
- scrollUntilVisible:
    element:
      text: "Clear Credentials"
    timeout: 10000
- tapOn: "Clear Credentials"
- scrollUntilVisible:
    element:
      text: "Yes, Clear"
    timeout: 15000
- tapOn: "Yes, Clear"
# Wait for confirmation that credentials were cleared from storage.
- extendedWaitUntil:
    visible: ".*Credentials cleared.*"
    timeout: 30000

# --- Restore credentials via seed-credentials so downstream SL-dependent
# flows on the same worker still have a working session. The seeded creds
# are real and come from server env vars — no secret via Maestro -e.
- openLink: ${APP_URL || "http://localhost:3000"}/testing/seed-credentials?redirect=/profile
- extendedWaitUntil:
    visible: "Profile Settings"
    timeout: 30000
