url: ${APP_URL || "https://localhost:3000"}/testing/sign-in?redirect=/dashboard&worker=${WORKER_INDEX || "0"}
name: Sign In
tags:
  - smoke
  - auth
---
- launchApp
- assertVisible: "Neon Binder"
- assertVisible: "Sign out"
