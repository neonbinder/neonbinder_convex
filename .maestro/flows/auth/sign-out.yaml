url: ${APP_URL || "http://localhost:3000"}/testing/sign-in?redirect=/dashboard
name: Sign Out
tags:
  - auth
---
- launchApp
- extendedWaitUntil:
    visible: "Neon Binder"
    timeout: 15000
- tapOn: "Sign out"
- assertVisible: "Get Started"
