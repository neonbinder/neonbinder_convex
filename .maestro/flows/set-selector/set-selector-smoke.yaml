url: ${APP_URL || "https://localhost:3000"}/testing/sign-in?redirect=/profile
name: Set Selector Smoke Test
tags:
  - smoke
  - set-selector
---
- launchApp
- extendedWaitUntil:
    visible: "Neon Binder"
    timeout: 45000

# --- Set up marketplace credentials for the test user ---
- runFlow:
    file: ../profile/setup-bsc-credentials.yaml
    env:
      BSC_USERNAME: ${BSC_USERNAME}
      BSC_PASSWORD: ${BSC_PASSWORD}

- runFlow:
    file: ../profile/setup-sportlots-credentials.yaml
    env:
      SPORTLOTS_USERNAME: ${SPORTLOTS_USERNAME}
      SPORTLOTS_PASSWORD: ${SPORTLOTS_PASSWORD}

# Navigate to the Set Selector page
- openLink: ${APP_URL || "https://localhost:3000"}/set-selector
- extendedWaitUntil:
    visible: "Set Selector"
    timeout: 15000

# Verify the Sport selector column is present
- assertVisible: "Sports"

# Scroll the Sync Sports button into view — Sports list may grow past the fold
# on accounts with existing data, and Maestro's web driver doesn't auto-scroll
# to find elements for assertVisible/tapOn.
- scrollUntilVisible:
    element:
      text: "Sync Sports"
    timeout: 10000
- assertVisible: "Sync Sports"
- assertVisible: "\\+ Custom"

# Trigger a marketplace sync for Sports
- tapOn: "Sync Sports"
- scrollUntilVisible:
    element:
      text: "Sync Sport Options"
    timeout: 5000
- scrollUntilVisible:
    element:
      text: "Sync from Marketplaces"
    timeout: 5000
- tapOn: "Sync from Marketplaces"

# Wait for sync to complete — the form closes on success and the idle
# Sync Sports button returns. scrollUntilVisible polls until the element
# appears anywhere in scrollable range, so it covers the wait implicitly.
- scrollUntilVisible:
    element:
      text: "Sync Sports"
    timeout: 45000

# Verify sports were synced from both platforms
# Entries from both BSC and SportLots should show their respective badges
- assertVisible: "BSC"
- assertVisible: "SL"
