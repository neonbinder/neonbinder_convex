# Maestro E2E Author — Memory Index

- ⭐ [Navigation: openLink, not link taps (maestro-web #2944)](feedback_navigation_openlink_convention.md): tapping a link that triggers navigation intermittently crashes maestro-web (`null→kotlin.Int` / `LinkedHashMap→String` cast in CdpWebDriver's post-tap DOM parse; surfaced as "Unknown error"). Default to `openLink` to navigate; assert href + openLink to verify a link; only `tapOn`-navigate when the click handler has real logic. Watch upstream #2944 for the fix.

- [tapOn does NOT auto-scroll on web headless (2026-06-20)](feedback_tapOn_autoscroll_experiment.md): 0/5 vs 5/5 — direct tapOn on below-fold elements always fails with "Element not found"; scrollUntilVisible is non-negotiable. Taller viewport (1024×900) reduces scroll time ~2.9s but does NOT eliminate the need.

- `clearState: true` on `launchApp` DOES clear the Clerk session (confirmed 2026-06-19, tracker #52). Worker left signed-in by sign-in flow → ai-identification with clearState → "Get Started" appeared unconditionally → CTA fully tested. Use `launchApp: { clearState: true }` in any flow that must start signed-out regardless of prior worker state.

- [Drill tap timing root cause](feedback_drill_tap_timing.md): `waitToSettleTimeoutMs: 0` has zero effect on tap duration. The 2s/tap is `hierarchyBasedTap` change-detection, not post-action settle. Do not tune settle expecting speedup; reduce action count instead.

- ⭐ [REFINED CRITERIA 2026-06-15 — READ FIRST](feedback_refined_criteria_2026-06-15.md): R4 flows NEVER log in (45s sign-in guard → 7s; a login block/>7s sign-in is a defect). R5 everything reacts in 7s — the ONLY long-wait exception is a step directly driving a LIVE marketplace fetch on non-pre-synced data; ALL Convex calls incl. feature propagation → 7s, >7s is a product FINDING not a pad (30s propagation/bulk-commit carve-outs RETIRED). R9 all set-drilling via `util-drill-to-2024-topps-chrome`/`util-drill-to-custom` (no hand-rolled inline drills). SUPERSEDES older 45s/30s long-wait entries for feature flows; setup track + credentials-lifecycle keep marketplace-auth waits.

- [Two-phase scroll pattern for in-viewport-zone elements](feedback_two_phase_scroll_pattern.md): For "Sync Sports" / "+ Custom" (aria-label "Add custom Sports") at y≈505 in the Sports column: (1) `extendedWaitUntil: visible: "Baseball"` (page-settled, non-scrolling) THEN (2) `scrollUntilVisible: id: "Add custom Sports" + centerElement: true`. The non-scrolling wait is critical — without it, CDP swipes fire while the page is mid-render (MismatchedInputException). Applies to any button in the bottom-of-column zone that is fully in-viewport (Visibility Percent: 1.0) but at y≥489. Sports sync error-path: after `extendedWaitUntil notVisible "Syncing Sport Options" timeout: 55000`, add a runFlow handler that taps "Cancel" or "Close" if form is still visible (38s FE backstop). See custom-entry-survives-resync.yaml.
- [when: true: env-var gating (confirmed JS semantics)](feedback_when_true_gating.md): `when: { true: "${VAR}" }` — unset var → JS `undefined` → falsy → block skipped. Set non-empty var → truthy → runs. Cumulative gate: `"${SPORT && YEAR && MANUFACTURER}"`. Source: GraalJsEngine.kt Proxy + IntegrationTest 065. NO quoted comparison needed. See file for full citation.
- [util-drill-to-custom.yaml Sports search — custom sport add](feedback_custom_sport_via_search.md): When a custom sport is NOT in the search results ("No matches found"), do NOT erase and re-scroll — use `scrollUntilVisible: id: "Add custom Sports" + centerElement: true` (button is at y≈520 in footer zone) then tap it directly from the "No matches found" state. After save, the search input retains the sport name and the new row appears below it — tap via `below: id: "Search sports"`. Do NOT re-type the name (it's still in the input). eraseText is unreliable here: the cursor-trap bug leaves 1 char, and the element reference goes stale during the 7s `when:` check window.

- [Set-selector conversion rules](feedback_set_selector_conversion_rules.md): Converting drilling flows to independent — (1) test WITH Phase-0 bootstrap (drilling needs a WARM token or the child column hangs; NOT SKIP_BOOTSTRAP); (2) centerElement to dodge the y≈556 footer-steal (fixed in util-drill); (3) reach /set-selector via a /profile detour so util-drill's openLink is a fresh cross-page mount (same-URL openLink doesn't remount; /dashboard detour stuck on "Loading…"); (4) select by `below:{id}` not index. Includes the conversion template.
- [Canonical pre-synced setup data](reference_presynced_setup_data.md): **NEO-62 UPDATED — READ FIRST.** The global setup TRACK = ONE FLOW (`setup.yaml`). Base + Insert + Parallel all run in a single browser context (collapsed from 3 flows by NEO-62 Lever 1 — no restarts, no re-logins). IMPORTANT: transitions between Base→Insert and Insert→Parallel use `runFlow: set-selector/util-drill-to-2024-topps-chrome.yaml` (fast re-drill via openLink /set-selector) — NOT an in-place switch. After a Base card fetch the CardChecklist fills the horizontal viewport, pushing Variant Types HORIZONTALLY OFF-SCREEN; scrollUntilVisible cannot reach horizontally clipped elements. The re-drill resets page state, leaving Insert/Parallel reachable via `scrollUntilVisible direction:DOWN`. Pre-syncs Baseball→2024→Topps→Topps Chrome with Base/Insert/Parallel, seeded Yankees/Mets. Every other flow is INDEPENDENT: fast-drill + read, NO `requires:`/`provides:` tags, NO global reset, default ~7s waits. Seed CI job runs with MAESTRO_SKIP_BOOTSTRAP=1 (setup.yaml is self-sufficient via its sign-in→reset→seed-credentials URL chain).

- [UI text and layout patterns](ui-patterns.md): Exact button text, column headings, empty states, and modal text for set-selector and profile credential pages. Includes the CRITICAL sync form auto-fire pattern, 6-level hierarchy, BaseSetPicker modal, ReconciliationModal, and VariantMetadataEditor. CRITICAL (PR #32): Level 6 column header/sync button/ReconciliationModal title are the PLURALIZED variantType — "Inserts" not "Variants" when Insert is selected; "Parallels" not "Variants" when Parallel is selected. aria-label for the + Custom button is "Add custom Inserts" / "Add custom Parallels" (never "Add custom Variants").
- [Auth and navigation patterns](auth-patterns.md): Testing sign-in URL pattern, reusable credential setup flows. CRITICAL: scroll anchor = `"BuySportsCards Credentials"` (h2); load anchor = `"Profile Settings"` (h1). Radix Button tapOn NPE (text match hits the inner span / null bounds) → fix by `scrollUntilVisible centerElement` + settle, then plain `tapOn` (the admin-missing-* pattern); NEVER `index:` (no-index rule, NEO-46); `id:`/aria-label if still failing. auth-failed banner = extendedWaitUntil 120s NO centerElement on Save; BSC heading scroll needs `timeout: 30000`; "Credentials cleared" needs `timeout: 30000`; reload /profile before Clear after cold login (reactive storm).
- [Search input selection (use below:, not index)](feedback_search_input_index_pattern.md): After typing in an EntitySelector search box, select the result ROW with `below: {id: "Search <entity>"}` — NOT `index: 1`. NEO-46 proved index is unreliable: the (input,row) DOM order flips between a cold sync and a re-drill, so `index:1` can land on the search input (year never selected → Manufacturers never opens → silent fail). Fixed in util-drill-to-2024-topps-chrome.yaml.
- [Sync form timing](feedback_sync_form_timing.md): CRITICAL — after DB reset, EntityColumn auto-syncs empty columns; "Sync X" button never reliably tappable. Guard on DATA indicators (e.g. `notVisible: "2024"`, `notVisible: ".*Search manufacturers.*"`), not button visibility. Full table of data indicators by level inside.
- [Sync button tolerance](feedback_sync_button_tolerance.md): For flows that OWN sync as a test assertion (not util/setup flows), use `scrollUntilVisible: "Sync X"` as completion signal — button reappears below list after all platforms merge. Never use data-item appearance (fires on first platform only).
- [Profile navigation before credential setup](feedback_profile_navigation_before_credential_setup.md): runFlow does NOT re-navigate; must explicitly openLink /profile before calling setup-*-credentials.yaml when starting from a non-profile URL.
- [Idempotent vs destructive credential helpers](auth-patterns.md): setup-*-credentials = idempotent (skip if present); clear-then-setup-*-credentials = always clears first. Use idempotent for feature flows, destructive only for cred-test flows that verify the save UI.
- [Native select workaround](reference_maestro_native_select_workaround.md): `tapOn: {id: "site-select"}` + `tapOn: "Sportlots"` works in CI (Linux Chrome). "Select Platform" label text is NOT visible in Maestro's DOM hierarchy. Confirmed passing in PR #24 run 25279139378.
- [2024 Topps Chrome anchor + EntitySelector column scroll](ui-patterns.md): Canonical test anchor is Baseball → 2024 → Topps → Topps Chrome (see util-drill-to-2024-topps-chrome.yaml). EntitySelector columns have INTERNAL scroll — page-level `scrollUntilVisible` cannot reach items inside; use the search box instead. ReconciliationModal save button is "Save N matched" (regex `".*Save [0-9]+ matched.*"`). SL column header is "SportLots (N of M)" mixed-case — NOT all-caps. Use `".*SportLots.*"` regex.
- [Idempotent input targeting](feedback_idempotency_input_fields.md): Never target inputs by placeholder text — it disappears once a value is saved. Use label text + eraseText instead.
- Custom entry post-Add verification: after clicking "Add" to save a custom entry in a column that already has marketplace data, the entry falls in the internal scroll below the fold. Cannot `assertVisible` directly. Correct pattern: wait for `notVisible: "Add Custom Entry"` (form close = save confirmed), then tap the search box and filter by the entry name.
- [Sync form timing — SetVariantForm and VariantForm exceptions](feedback_sync_form_timing.md): CORRECTED: SetVariantForm auto-closes on success (result.success===true); "Close" only appears on result.success===false (error path, unachievable from UI). Do NOT assert "Close" after re-sync. (2) VariantForm (Insert/Parallel) won't open ReconciliationModal on re-sync with partial data; guard on empty-state text.
- AdminTools Reset panel UI: wait anchor is `"Delete Everything"` button with `timeout: 20000` (NOT 5000 — tapOn "Reset Set Builder Data" can take 6s retrying, leaving only 4s for panel animation, causing "Delete Everything" to time out). Input has `autoFocus`; use `eraseText: 10` then `inputText: "RESET"` — NEVER `tapOn: "Type RESET"` first (the tapOn gesture injects a stray "p", producing "pRESET" which disables the Delete Everything button). Success regex: `".*Deleted [0-9]+ selector options.*"`. Error: `"Reset failed: ..."` with `"Dismiss"` link. BSC auth timeout: use 120s (local runs recorded ~65s); Sportlots: use 90s (recorded ~50s).
- [ParallelGroupingModal UI text](ui-patterns.md): "Group Parallels" button: use `scrollUntilVisible: element: text: "Group Parallels"` + `tapOn: "Group Parallels"` (plain visible text works; no id needed). Modal description regex `".*Drag inserts under a parent.*"` is the ONLY reliable modal-open signal. Initial state WITH suggestions: footer shows "N promotions" immediately. "Accept all suggestions (N)" removes Suggested badges but doesn't change counts. ✕ reject button: aria-label is `"Remove <value> from parallels"` — use `tapOn: {id: "Remove Stars Gold from parallels"}` (aria-label maps to resource-id). Controlled test data MUST have 3+ inserts before tapping Group Parallels (2 inserts = button partially offscreen). Use "Stars"/"Stars Gold"/"Stars Red" for 2-suggestion flows, "Stars"/"Stars Gold"/"Champions" for 1-suggestion flows.
- [Maestro web aria-label / resource-id resolution](ui-patterns.md): `aria-label` on web elements maps to `resource-id` in Maestro's accessibility hierarchy — NOT to the `text` attribute. `tapOn: "aria-label-value"` (plain string) will FAIL. `tapOn: {id: "aria-label-value"}` WORKS because `id:` matches resource-id. `scrollUntilVisible` with `element: text:` only matches visible DOM text, not aria-label. Plain button text (e.g. "Group Parallels", "+ Custom" visible text) IS matched by text matchers. `pressKey: Escape` does NOT fire window-level keydown events — cannot test modal Escape closure via Maestro web. CONFIRMED BROKEN: `pressKey: Tab` throws `IllegalStateException: Keycode TAB is not supported on web` — never use Tab in Maestro web flows. `pressKey: Enter` DOES work and fires the focused element's click handler.
- [PR #25 regressions: Base terminal + auto-sync Variants + no pills on non-terminal](feedback_sync_form_timing.md): Base variantType suppresses Variants column; never wait for "Variants" after tapping "Base". Auto-sync fires ReconciliationModal without needing tapOn "Sync Variants". SL/BSC pills gone from non-terminal levels — use "Baseball" to assert Sports sync worked. CardChecklist for Base attaches to the variantType row (no Level 6 Variants column). See also [ui-patterns.md](ui-patterns.md) for updated column visibility rules.
- CardChecklist "Cards" heading unreliable for scrollUntilVisible — use `"No cards in this checklist yet."` (empty state) or `"Fetch from Marketplaces"` instead. Cancel text is `"Cancel (Esc)"` NOT `"Cancel"`. Confirm button regex: `".*Confirm [0-9]+ & Save.*|Skip All & Save"`. Escape key CANNOT be tested (document-level listener). See [ui-patterns.md](ui-patterns.md).
- Multi-column "+ Custom" index rule: When Insert is selected with Variants column visible, 3 columns each have "+ Custom". Use `index: 2` (Variants column) not `index: 0` (Sets col). Wrong index adds an entry to the wrong hierarchy level. See [ui-patterns.md](ui-patterns.md).
- [CardChecklist flow patterns](feedback_card_checklist_patterns.md): "Refresh" scroll needs `speed: 10 + waitToSettleTimeoutMs: 300 + timeout: 120000` past 335 rows. BaseMappingForm dual-path. SL badge dual requirement. Print-run only in parallels. UnknownEntitiesDialog requires empty players/teams tables (fresh CI DB). BSC "No BSC token" error modal at Sets/Variant Types when creds absent — dismiss with "Close", then use `+ Custom` at index 2 (Sets) or 3 (Variant Types). Empty state text needs `".*No sets available.*"` (partial match). BSC teardown contamination pattern: see file.
- [YAML escape rules for regex](feedback_yaml_escape_rules.md): `\(` and `\)` are INVALID YAML escapes — use `[(]` and `[)]` instead. `\/` is also invalid — use `/` directly. Causes parse-time failure, not assertion failure.
- Transient "Fetching..." state: only assert `extendedWaitUntil: visible: "Fetching..."` in flows where the fetch takes measurable time (both platforms active, network round-trips). When one or both platforms return instantly (e.g. BSC credentials absent, SL no set mapping), the state is too transient to assert — go straight to waiting for the terminal state (`".*Saved 0 cards.*"` or similar).
- BaseMappingForm post-confirm timing: after `tapOn: "Confirm Base Set"`, wait 30s (not 15s) for "Fetch from Marketplaces" — the Convex write + reactive update chain (`writePlatformData` → `baseHasMapping=true` → CardChecklist renders) can take 15-20s. `util-drill-to-base-variant.yaml` uses 30s; the BaseSetPicker modal appearance timeout should be 60s (not 20s) to cover SL Puppeteer session re-auth (~50s) if session lapsed between credential save and modal use.
- Credential teardown contamination (RESOLVED in PR #53): `test-bsc-credentials.yaml` + `test-sportlots-credentials.yaml` deleted; replaced by `test-credentials.yaml` (BSC+SL together, no teardown) + `credentials-lifecycle.yaml` (isolated new-profile account, full lifecycle). No more clearance in a shared-account teardown. The 30s "Credentials cleared" wait is in credentials-lifecycle.yaml only, not in test-credentials.yaml.
- AdminTools reset timeout: `extendedWaitUntil: visible: ".*Deleted [0-9]+ selector options.*" timeout: 60000` — in local dev, after saving 335 cardChecklist rows + 100 selectorOptions + players + teams, the reset can take >60s (Convex read-limit error returns "Reset failed:" instead of success). CI preview deploys start fresh and won't hit this. For local large-DB resets, the success message may be "Deleted 0 selector options and 0 card checklist rows" (if selectorOptions already empty), which DOES match the regex.
- Post-reset scroll: after `resetSetBuilderData()` completes, the EntitySelector below AdminTools auto-syncs Sports (now empty DB), which causes a reactive re-render that scrolls the viewport DOWN. AdminTools (at page top) ends up above the fold. Any `scrollUntilVisible` for an AdminTools element that runs AFTER the reset must use `direction: UP + centerElement: true`. Without this, the default direction: DOWN searches away from AdminTools and times out (run 26478848720). Applies to "Seed Test Teams" and any other AdminTools button used after a reset.
- [Cascade pattern (set-selector)](cascade-pattern.md): POST-PR31: cascade/ has setup.yaml → sets-base/inserts/parallels/resync-already-loaded (sets-loaded) → cards-base/insert/parallel (cards-loaded). cards-parallel-of-insert.yaml deleted (deferred — Group Parallels modal close leaves React state where Maestro inputText fails to retrigger filter onChange). cards-insert saves Future Stars (20 cards), cards-parallel saves Gold Wave Refractors (300 cards). All cascade cards-* flows use `inputText` into the column search input + `text: "^Foo$"` regex + `index: 1` to pick the bare row over its parallel siblings, then `tapOn: ".*Fetch from Marketplaces.*|^Refresh$"` so reruns hit the same path.
- [Custom playground isolation](cascade-pattern.md): Custom-data flows (parallel-grouping-*, variant-metadata-editor-insert, move-parallels-of-inserts-custom, cards-parallel-custom) use `util-drill-to-custom-set.yaml` with **E2E Test Sport ${WORKER_INDEX} → 2026 → Topps → per-flow SET_NAME** — ALL FOUR levels are custom (incl. a custom "Topps" manufacturer; the once-custom-always-custom rule removed the manufacturer exemption). The per-worker sport name keeps concurrent workers isolated. The util adds each level as a custom entry (idempotent) and returns at the Variants column idle. Custom subtrees short-circuit BSC/SL adapters instantly via the NEO-22 `isCustomSubtree` gate. NOTE (NEO-46): the cluster was consolidated feature-first — `cards-insert-custom`, `cards-parallel-of-insert-custom`, `cards-custom-subtree-gate` were DELETED (they only re-asserted the variant-agnostic fetch gate); `cards-parallel-custom` was refocused to assert the add-parallel FEATURE (entry appears + Custom badge + selectable), not the gate.
- [CDP scroll-bug + Custom-badge assertion](reference_cdp_scroll_bug_and_badge.md): (1) `scrollUntilVisible` throws `MismatchedInputException: No content to map` at `CdpWebDriver.scroll` while the page is MID-RE-RENDER (e.g. just after a fresh custom set is created+selected) — retries the whole timeout then fails ElementNotFound even though the target is in-viewport. It's intermittent (passes when settled) and it's the CI-representative path (setup resets data each run → always fresh). Fix: for in-viewport targets (short columns, empty states, a 0-item column's "+ Custom") use a NON-scrolling `extendedWaitUntil visible`/`assertVisible`; only `scrollUntilVisible` for genuinely below-fold panels AFTER the triggering tap settles. (2) The "Custom" badge is a SEPARATE span from the value, so `".*VALUE.*Custom.*"` matches nothing — assert `text: "Custom"` + `rightOf: {text: "VALUE"}`.
- [TeamPicker, CardFeaturesEditor, SetAttributesPanel UI patterns](ui-patterns.md): NEO-26/24/38 components. TeamPicker: `id: "Add team"` opens popover, `id: "Search teams"` input, match buttons by visible team name text, remove chip `id: "Remove team <name>"`. CardFeaturesEditor (per-card, unchanged): `id: "Show features editor"` to expand, `id: "Value for League"` for inputs, `id: "Revert League to inherited"` for revert, `id: "Missing required feature"` for amber ⚠. SetAttributesPanel (NEO-38 rename of SetFeaturesPanel): container aria-label is now `"Set attributes panel"`, heading is `"Set attributes"` (was `"Set features"`). COLLAPSIBLE — defaults collapsed when card checklist is on screen; `id: "Edit attributes"` toggle to expand, `id: "Hide attributes"` to collapse. Defaults EXPANDED at setName level (no card checklist). Breadcrumb scope text: `"Attributes for {value} ({Level}) — A › B › C"`. setName-level editable metadata: `id: "Value for Release Date"`, `id: "Value for Total Cards"`, `id: "Value for Block"`. Feature inputs: `id: "Set feature League"` rows, `id: "Value for League"` inputs. Toast text: "Updated N cards" (features); "Saved Release Date" (metadata). Empty setMetadata: "No set metadata yet — pending sync." ALWAYS insert idempotent expand step before touching any feature input: `runFlow: when: visible: id: "Edit attributes" commands: [tapOn: id: "Edit attributes", extendedWaitUntil: visible: "Set attributes" timeout: 5000]`. CRITICAL: SetAttributesPanel.MetadataEditableRow + SetFeatureRow both use plain `<input>` without `mb-field-*` unique class (they use useReactiveField directly, not ReactiveTextField). Until the product adds useFieldTestClass() to both row types, inputText after tapOn id: "Value for X" will misfire to the FIRST visible input. This is the root blocker for set-attributes-edit.yaml (PR #48).
- Double-write pattern for SetAttributesPanel idempotency: write sentinel value first (triggers toast), then write intended value. Prevents false no-op when DB already has the intended value from a prior test run. See set-features-panel.yaml (targets SetAttributesPanel since NEO-38 rename) and features-propagation.yaml.
- PropTest- prefix convention: features-propagation.yaml uses "PropTest-MLB"/"PropTest-NL"/"PropTest-AL" as League values to distinguish from smoke test's "MLB" and avoid false idempotency (old value equals new value → mutation is no-op → no toast).
- Card number ranges: test cards use 700-999 range (777, 888, 889, 991, 992). Each flow uses a unique number and deletes its card at end of flow. Never conflicts with marketplace cards (start at 1).
- eraseText cursor trap (Maestro web): `tapOn: id: "some-input"` places cursor at the PENULTIMATE position of a React controlled input (position N-1, not N). `eraseText: K` erases backward, leaving the last character after cursor. Fix: (1) use `eraseText: 60+` (larger than field length) + accept that 1 trailing char remains, (2) use `text: ".*expected value.*"` regex in assertions where the trailing char is from Maestro behavior not a product bug. For card-detail-panel.yaml: `eraseText: 60` + `assertVisible: text: ".*Renamed Detail Player ${ATTEMPT_ID}.*"` (regex suffix). See `patterns_scroll_visibility.md` for the broader eraseText doc.
- Virtuoso virtualization post-submit: after `tapOn: id: "Submit new card"`, the new card (e.g. #888) sorts to the BOTTOM of the list and is NOT rendered in the initial viewport. Do NOT use `extendedWaitUntil: visible: "<player name>"` — the row isn't in the DOM yet. Go straight to `scrollUntilVisible: id: "Edit card 888-..."` which materializes the row. Same pattern applies after Save (drawer close): use `extendedWaitUntil: notVisible: id: "Card name"` to confirm close, then `scrollUntilVisible` for the edit button.
- Maestro assertVisible textRegex is FULL-STRING anchored: `assertVisible: "foo bar"` generates `textRegex: "foo bar"` which requires the node's text to be EXACTLY "foo bar". If the stored value has a trailing char (e.g. "foo bar8"), the assertion fails. Use `assertVisible: text: ".*foo bar.*"` for prefix/contains matching.
- [evalScript runs in GraalJS, not the browser](feedback_evalscript_graaljs.md): CRITICAL — `evalScript` runs in Maestro's GraalJS JVM sandbox (no `window`/`fetch`/`Clerk`); use `http.post()`/`maestro.copiedText`/`output.*`. Bridge browser values via `copyTextFrom` → `maestro.copiedText`.
- [No secrets via Maestro -e](feedback_no_secrets_via_env.md): Hard rule (NEO-29) — never put `${BSC_*}`/`${SPORTLOTS_*}` in flows; they leak into public CI artifacts. Use fake literals for form flows, server-side seed-credentials for real-auth flows.
- [Credential seeding patterns](patterns_credential_seeding.md): Post-NEO-29 — fake creds for FORM coverage (Group A), seed-credentials chain for real-auth (Group B), reset-then-seed for worker bootstrap (Group C), seed-only for set-selector (Group D). Includes verified auth-failed assertion strings.
- [URL redirect chain patterns](patterns_url_redirect_chains.md): Nested `?redirect=` chains — seed-only (most flows), reset-then-seed (worker bootstrap), reset-only (existing). Always preserve `&worker=${WORKER_INDEX || "0"}`.
- Footer click-stealer on Sets-level row tap (PR #31, 2026-05-22): worker-2 Sets column accumulates one entry per flow (alphabetical sort). SET_NAMEs sorting late (pg-suggestions-2, vme-insert-2) land at y≥489 in the 1024×629 headless viewport — footer paints over and absorbs the click; the DOM element is found at e.g. y=489 but React's onClick never fires (Variant Types column never opens). Fix: `scrollUntilVisible` for the SET_NAME tap must use `centerElement: true` to put the row at mid-viewport (~y=315). Diagnosis: every PASSED tap landed at y≤457, every FAILED tap landed at y≥489. Also see `reference_maestro_headless_viewport.md`.
- [centerElement: true on every scroll to a tap target](feedback_center_element_on_tap_targets.md): Maestro best practice (generalizes the footer-steal fix above) — ANY `scrollUntilVisible` whose target is later `tapOn`-ed must use `centerElement: true` (drop `visibilityPercentage` on those steps). Centers at ~y=315, clear of the fixed nav (top) and the footer-steal zone (y≥489) where the DOM tap registers but React onClick never fires. Scroll-to-ASSERT steps don't need it. Corollary (no conflict with the CDP rule): if the tap target is already in-viewport, prefer a NON-scrolling wait instead. CAVEAT: centerElement does NOT fix data-instability — an element *found then vanished* mid-flow is a concurrency wipe, not a scroll problem (reproduce solo first).
- addCustomSelectorOption select-existing pattern: When the user types a value that ALREADY EXISTS as synced marketplace data into the "+ Custom" form, the server returns the existing row's _id and the FE calls `onSelectExisting(_id)` — same as tapping the row directly. The form closes (`notVisible: "Add Custom Entry"`) and the drill advances normally. Flow guard: assert `notVisible: "Add Custom Entry"` (form close = accepted), then `assertVisible: "Sync Years"` (NOT `"Years"` — the column heading scrolls above the fold once year rows populate; "Sync Years" stays in-viewport and is unique to the Years column). No need to assert no-duplicate (unit test covers that). See `custom-field-known-value-selects.yaml`. The "Add custom Sports" button auto-focus means NO `tapOn: placeholder` before `inputText` is needed — the form's input receives focus on mount.
- "Add custom X" button taps after util-drill-to-custom-set.yaml MUST use centerElement: true (2026-05-30, run 26693637566): util-drill-to-custom-set.yaml returns with the Inserts/Parallels/Sets button near y=589 in the 1024×629 viewport. `visibilityPercentage: 50` triggers COMPLETED but the tap is stolen by the footer zone. Confirmed pattern: scrollUntilVisible COMPLETED → tapOn "Element not found" 17s later. Fix applied in cards-custom-subtree-gate.yaml ("Add custom Sets") and all four parallel-grouping-*.yaml flows ("Add custom Inserts", first Stars tap). util-drill-to-custom-set.yaml already had the correct centerElement: true for its own "Add custom Sets" tap — it was only the CALLERS that were missing it.
- [CardDetailPanel sticky elements MUST NOT use scrollUntilVisible](ui-patterns.md): CardDetailPanel (`flex flex-col` panel) has a sticky footer div OUTSIDE the `flex-1 overflow-y-auto` scroll body. Footer contains: "Cancel card edit" / "Save card edit" buttons (normal state) OR "Keep editing" / "Discard changes" buttons (dirty-guard state, shown when pendingAction is set). Header contains "Close card detail", "Previous card", "Next card". NONE of these should use `scrollUntilVisible` — they are always visible in the fixed footer/header of the panel and `scrollUntilVisible` times out trying to center them (CI run 2 root cause on card-detail-panel.yaml). Use `extendedWaitUntil: visible: id: "<aria-label>"` + plain `tapOn: id:` for all footer/header buttons. Only use `scrollUntilVisible` for elements INSIDE the scroll body (e.g. "Toggle RC" chip at position 6).
- [scrollUntilVisible centerElement + visibilityPercentageNormalized=0.0 trap](patterns_scroll_visibility.md): When `centerElement: true` is used and `visibilityPercentageNormalized` reports 0.0 (CDP hit-test clipping), the centering loop runs indefinitely. For fields below the fold that report this pattern (metadata rows in SetAttributesPanel, deep-grid inputs), omit `centerElement` — the scroll still scrolls the element into view, and `Visibility Percent: 1.0` satisfies the `visibilityPercentage: 10` threshold without centering. Add `visibilityPercentage: 10 + waitToSettleTimeoutMs: 1000` to prevent the default 100% threshold from throwing CDP errors at edge elements.
- Dual-branch `when:` firing bug in drill utils: In util-drill-to-custom.yaml, each level has a "search-present" branch and a "no-search-input / custom" fallback. After the search branch selects an item, the search input disappears from DOM — making the fallback's `when: notVisible: search-input` TRUE, causing it to fire erronously. Also, WITHIN the search branch, the "not found" sub-branch's `when: notVisible: item, below: search-input` fires after success because both the item AND search disappear post-selection. Fix: guard EVERY fallback with `when: notVisible: "NEXT_COLUMN_HEADER"` (e.g. `when: notVisible: "Years"` for L1, `notVisible: "Manufacturers"` for L2, `notVisible: "Sets"` for L3). This is the pattern used in Level 4's fallback (`when: notVisible: "Variant Types"`). Applied in util-drill-to-custom.yaml L1/L2/L3 during team-picker validation.
- Local env infra: ALWAYS use `export JAVA_HOME=/opt/homebrew/opt/openjdk@21` (OpenJDK 23 from Homebrew crashes with SIGSEGV in G1GC during Maestro runs). Vite requires Node 24 via `nvm use` (project `.nvmrc`=24.3.0; Node 22 fails mkcert/undici). Vite serves HTTPS at `https://localhost:3000` (not http) — use `curl -sk --insecure` for health checks. Background processes started in Bash tool calls die when that shell exits; start Vite in a separate terminal or via `nohup ... & disown` — BUT disown requires a job that actually backgrounds (check `ps aux | grep vite` to confirm process is alive).
- Sports re-sync duration: local dev cold-session BSC+SL sports sync takes 60-120s (Puppeteer re-auth). `extendedWaitUntil: notVisible: "Fetching the latest sport options..."` needs `timeout: 120000` for local dev. Better: test that sync STARTED (`extendedWaitUntil: visible: "Syncing Sport Options" timeout: 15000`) then navigate away — doesn't require waiting for full completion.
- CDP crash in util-login-to-sportlots.yaml (bootstrap JVM kill): `scrollUntilVisible: "Sportlots" centerElement:true` crashes the JVM with `MismatchedInputException: No content to map due to end-of-input` when the page is mid-Convex subscription re-render (the second /profile navigation in bootstrap triggers a fresh Convex update that races with the centering swipe). Fix: remove `centerElement:true` from the "Sportlots" scroll and use `waitToSettleTimeoutMs: 2000` (incremental scrolls respect the settle delay; the centering swipe does not). File: `.maestro/flows/util/util-login-to-sportlots.yaml`.
- ATTEMPT_ID MUST be a PREFIX in move-parallels flows: `"${ATTEMPT_ID} Stars"` NOT `"Stars ${ATTEMPT_ID}"`. The auto-suggestion algorithm groups inserts by common prefix — old accumulated bare `"Stars"` entries (from runs before ATTEMPT_ID was added) ARE a prefix of `"Stars w0-a1-X Gold"`, so the algorithm groups the new run's triplet under the OLD parent. Result: `"Stars w0-a1-X"` itself gets promoted to parallel-of-"Stars", disappears from Inserts column, post-save assertion fails. With ATTEMPT_ID as prefix (`"w0-a1-X Stars"`), no old entry matches as parent. Rule: always put ATTEMPT_ID first when using test-data-naming that the auto-suggestion algorithm will evaluate.
- [Crowded column scroll + modal body assertions](feedback_crowded_column_scroll.md): EntitySelector internal overflow-y-auto cannot be scrolled by CDP page-level swipes. Tap target in crowded list (>8 items): use `runFlow: when: visible: id: "Search inserts" commands: [tapOn id, inputText, tapOn below id]`. Clean CI (≤8 items): `runFlow: when: notVisible: id: "Search inserts" commands: [scrollUntilVisible + tapOn]`. Column scroll reset: `/dashboard` → wait "COMING SOON" → `/set-selector` → wait "Baseball" → util-drill (pre-warm stops auto-sync from firing again). Modal body rows: NEVER assertVisible in a ParallelGroupingModal body — footer-only assertions ("Accept all suggestions", promotion count, Save button) are the reliable signals.
- credentials-lifecycle sticky-header trap: `scrollUntilVisible ".*Sportlots account authenticated successfully.*"` (no centerElement) after centering "Test Credentials" at y=258 leaves credential section near TOP (auth success at y=109). Subsequent "Clear Credentials" at y=36 → "Yes, Clear" confirmation at y=19 → tap absorbed by sticky NeonBinder nav header (y=0-60). Fix: use `extendedWaitUntil: visible:` for ALL auth-success banner waits that are followed by a Clear action — this keeps the viewport at the centered "Test Credentials" position so the confirmation dialog appears at y≈367. `extendedWaitUntil` works here because the success banner appears IN-VIEWPORT at the credential section where "Test Credentials" was tapped.
- `extendedWaitUntil: visible:` CANNOT find off-screen (below-fold) elements in Maestro web. It only sees elements currently in the viewport. Do NOT use it as a "DOM settled" guard for elements like "BuySportsCards Credentials" heading that require scrolling. That pattern FAILS with 30s timeout ("Assertion is false: X is visible"). Use `scrollUntilVisible` with timeout:30000 for below-fold elements — the CDP errors retry and eventually settle.

- [QR + Sale flows (NEO-53/54)](reference_qr_sale_flows.md): cloud queue regenerates TEST_USERNAME FRESH PER FLOW + has NO requires/provides → flows must be SELF-CONTAINED (each signs in new-profile + resets + fills its OWN profile via util-tagged runFlow `profile/util-fill-public-profile.yaml`, NOT a producer/consumer chain — local single-username masks the bug). getMyPublicProfile is per-signed-in-user (generate-qr uses account=new-profile); QR content renders below the 1024x629 fold (scroll to Download before asserting URL <p>); amount input via visible "Sale Amount" label NOT id:amount; reset-on-pay NOT testable (payment tap window.opens external tab).
